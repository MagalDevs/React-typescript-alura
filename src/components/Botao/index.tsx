import React, {Component} from 'react';

class Botao extends Component {
    render() {
        return (
            <button>
                botão da massa
            </button>
        );
    }
}

export default Botao;